# Vendored binaries — fetched and staged at runtime by Install-Prereqs.ps1
tools/bin/
tools/kev/
# PS modules are committed directly (small, no build step required)

# PurpleKnight split parts and reassembly script are committed;
# the reassembled exe itself is excluded (245 MB — over GitHub limit)
!tools/bin/PK_Community_5.0/PurpleKnight.exe.partaa
!tools/bin/PK_Community_5.0/PurpleKnight.exe.partab
!tools/bin/PK_Community_5.0/PurpleKnight.exe.partac
!tools/bin/PK_Community_5.0/PurpleKnight.exe.partad
!tools/bin/PK_Community_5.0/Restore-PurpleKnight.ps1

# Raw run artifacts (large zips / XML)
output/runs/*/artifacts/

# Do NOT ignore output/runs/*/  — JSON records are the source of truth and must be committed
!output/runs/
!output/runs/**/*.json

# Local config overrides
config/settings.local.psd1
config/targets.psd1

# PurpleKnight exports — sensitive AD security data; store locally, do not commit
output/purpleknight/*.csv
output/purpleknight/*.html
output/purpleknight/*.xlsx
!output/purpleknight/.gitkeep

# OS / editor noise
.DS_Store
Thumbs.db
*.bak
*.tmp
